apiVersion: v1
kind: Service
metadata:
  labels:
    app: realworld-app
  name: backend-service
  namespace: realworld-app
spec:
  type: NodePort
  ports:
  - targetPort: 80
    port: 80
    nodePort: 30008
  selector:
    app: realworld-app
    type: be