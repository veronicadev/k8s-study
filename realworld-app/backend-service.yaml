apiVersion: v1
kind: Service
metadata:
  labels:
    app: realworld-app
  name: backend-service
  namespace: realworld-app
spec:
  type: NodePort
  ports:
  - targetPort: 8080
    port: 8080
    nodePort: 30008
  selector:
    app: realworld-app
    type: be